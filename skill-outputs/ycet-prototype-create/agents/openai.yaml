interface:
  display_name: "YCET Prototype Create"
  short_description: "需求完善、UI 确认、独立 HTML 原型制作与可视化工作台"
  default_prompt: "Use $ycet-prototype-create to route my prototype task and follow its confirmation and single-file rules."
policy:
  allow_implicit_invocation: true
